name: Docker Reusable Workflow
on:
  workflow_call:
    inputs:
      dockerimage:
        default: spring
        type: string
      DOCKERHUB_USERNAME:
        type: string
        default: alammanoj
        required: true
    secrets:
       username:
         required: true
       password:
         required: true
       SONAR_TOKEN:
         required: true
jobs:
  build_and_publish:
    runs-on: ubuntu-latest
    steps:
      - name: checkout the repo
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - name: setup the java
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: 17
          cache: maven
      - name: Build with maven
        run: mvn clean package -DskipTests
      - name: SonarQube Analysis
        run: >
         mvn org.sonarsource.scanner.maven:sonar-maven-plugin:sonar
         -Dsonar.projectKey=ccaabe4cf6b27e79f53fc31b846ca18c2158420c
         -Dsonar.organization=alammanojkumar
         -Dsonar.host.url=https://sonarcloud.io
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
      - name: Install the docker 
        uses: docker/setup-buildx-action@v4
      - name: login to dockerhub
        uses: docker/login-action@v4
        with:
          username: ${{ secrets.username }}
          password: ${{ secrets.password }}
      - name: Build and publish Image master
        if: ${{ github.ref_name == 'master' }}
        uses: docker/build-push-action@v4
        with: 
          context: .
          push: true
          tags: ${{inputs.DOCKERHUB_USERNAME}}/${{inputs.dockerimage}}:${{ github.run_id }}
      - name: Build and publish image snapshot 
        if: ${{ github.ref_name != 'master' }}
        uses: docker/build-push-action@v4
        with:
          context: .
          push: true
          tags: ${{inputs.DOCKERHUB_USERNAME}}/${{inputs.dockerimage}}-snapshot:${{ github.run_id }}

